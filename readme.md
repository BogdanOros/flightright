Flightright **Member service**

1) All endpoints are secured with Basic Auth. Use credentials (admin:password)
2) swagger at /swagger-ui.html
3) File uploading works for **local** profile (enabled by default)